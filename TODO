 ☐ implement better tee fn + tests

# defmacro tee(args, func) do
#   quote bind_quoted: [args: args, func: func] do
#     (fn ->
#       args |> func
#       {:ok, args}
#     end).()
#   end
# end

